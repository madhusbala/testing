// histogram.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
int sorting(int c[50]);

int _tmain(int argc, _TCHAR* argv[])
{
	int c[50];
	int temp=0,i,j,l,g=0;	
	printf("Enter the number of elements to be given:");
	scanf("%d",&l);
	printf("Enter the numbers:");
	for(int h=0;h<l;h++)
	{
		scanf("%d",&c[h]);
	}
	
	sorting(c[50]);
	for(i=0;i<l;i++)
	{
		printf("%d ",c[i]);int a=c[i];
		while(a>0)
		{
			printf("*");a--;
		}
	printf("\n");
	}


	int sorting(int c[50])
	{
		for(i=0;i<l;i++)
		{g++;
		for(j=g;j<l;j++)
		{
			if(c[i]>c[j])
			{
				temp=c[i];c[i]=c[j];c[j]=temp;
			}
		}
		}
	}

}